import {Type} from "../models/type";

export const fire = new Type({
    name: "Fire"
});

export const water = new Type({
    name: "Water"
});

export const plant = new Type({
    name: "Plant"
});

export const electric = new Type({
    name: "Electric"
});